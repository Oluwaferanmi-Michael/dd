enum UserRank {
  unranked,
  rookie,
  bronze,
  silver,
  gold,
}
enum TaskStatus {
  undefined,
  pending,
  done,
  didNotFinish,
  complete
}
enum TaskPriority {
  focusNow,
  nextInLine,
  whenYouCan,
  forLater,
  someday,
}