import 'package:flutter/material.dart';

class AlertDialog {
  void present(BuildContext context) {
    showDialog(
        context: context,
        builder: (context) {
          return Container();
        });
  }
}
